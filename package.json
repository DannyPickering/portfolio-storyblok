{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "dev:https": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 nuxt dev --https --ssl-cert localhost.pem --ssl-key localhost-key.pem",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.0.0",
    "@storyblok/nuxt": "^5.4.0",
    "cross-env": "^7.0.3",
    "nuxt": "^3.3.3",
    "sass": "^1.62.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "gsap": "https://majestic-souffle-62b882.netlify.app/gsap-bonus.tgz"
  }
}
